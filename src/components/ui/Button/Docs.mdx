import ButtonStories from './Button.stories';
import {
  Meta,
  Title,
  Story,
  Controls,
  Canvas,
  Source,
  Primary,
} from '@storybook/addon-docs/blocks';

<Meta of={ButtonStories} />

<Title />

The button component is a simple wrapper around the native HTML button element.
It accepts the following props as well as the native HTML button element props.

## Usage

The button component should be used to trigger actions or events such as
submitting a form or opening a modal.

<Canvas sourceState="shown" />
<Controls />
